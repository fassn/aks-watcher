BASE_URL=http://localhost:3000
NEXT_PUBLIC_LOCALE=fr-FR
NEXT_PUBLIC_TIMEOUT_BETWEEN_QUERIES=3000

# Matches docker-compose.yml defaults
DATABASE_URL=postgresql://aks:aks@localhost:5433/aks_watcher

# Generate hash easily: openssl rand -base64 32
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000
# Local mail catcher (docker compose service)
EMAIL_SERVER=smtp://localhost:1025
EMAIL_FROM=noreply@aks-watcher.local

# Cloudinary credentials
CLOUD_NAME=
API_KEY=
API_SECRET=
